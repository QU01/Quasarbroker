quasarbroker:
  controllers:

    backend:
      type: deployment
      strategy: RollingUpdate
      rollingUpdate:
        unavailable: 1
      containers:
        main:
          securityContext:
            runAsUser: 1001
            runAsGroup: 1001
          image:
            pullPolicy: Always
            repository: ghcr.io/bigbodycobain/quasarbroker-backend
            tag: latest
          env:
            AIS_API_KEY:
              secretKeyRef:
                name: quasarbroker-env
                key: AIS_API_KEY
            OPENSKY_CLIENT_ID:
              secretKeyRef:
                name: quasarbroker-env
                key: OPENSKY_CLIENT_ID
            OPENSKY_CLIENT_SECRET:
              secretKeyRef:
                name: quasarbroker-env
                key: OPENSKY_CLIENT_SECRET

    frontend:
      type: deployment
      strategy: RollingUpdate
      rollingUpdate:
        unavailable: 1
      containers:
        main:
          securityContext:
            runAsUser: 1001
            runAsGroup: 1001
          image:
            pullPolicy: Always
            repository: ghcr.io/bigbodycobain/quasarbroker-frontend
            tag: latest

          env:
            BACKEND_URL: 'http://quasarbroker-backend.quasarbroker.svc.cluster.local:8000'

  service:
    backend:
      controller: backend
      type: ClusterIP
      ports:
        http:
          port: 8000
    frontend:
      controller: frontend
      type: ClusterIP
      ports:
        http:
          port: 3000
